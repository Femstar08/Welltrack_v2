# Ralph Progress Log — Phases 11-15 Autonomous Run
Started: 2026-03-28
Branch: ralph/phase-11-to-15-autonomous

## Codebase Patterns
- Use `cmd.exe /c` to run Flutter commands on WSL2
- Must pass `--dart-define-from-file=.env` for Supabase credentials
- All navigation uses GoRouter — NEVER use Navigator.pushNamed()
- Dashboard route is `/` not `/dashboard` — use `context.go('/')`
- `wt_users` has NO email column — get email from `auth.currentUser`
- All Supabase calls must go through repository classes
- Riverpod providers: use .family keyed by profileId for user-scoped state
- After mutations, invalidate downstream providers with ref.invalidate()
- AI calls go through ai_orchestrator_service.dart — never direct from screens
- Error handling: wrap async in try/catch with AppLogger
- All entities use Freezed for immutability
- Use context.push() for stack navigation, context.go() for replacement
- All screens pushed via context.push() MUST have AppBar with back button
- PrescriptionEngine is static class, not a provider
- Recovery score accessed via insightsProvider, not standalone provider
- `FreemiumGate` internally uses Navigator.push — new widgets should use `context.push('/paywall')` directly
- Freemium gates use `currentPlanTierProvider` (FutureProvider)
- `onboarding_completed` is on `wt_users`, NOT `wt_profiles`
- minSdk = 28, AGP 8.11.1, Kotlin 2.2.20
- GarminAttributionWidget/StravaAttributionWidget use `visible` param (default true)
- healthConnectionsProvider is family keyed by profileId, returns HealthConnectionsState
---

## 2026-03-28 - P11-001
- Removed isComingSoon=true from Garmin and Strava cards in health_connection_screen.dart
- Replaced static _buildConnectionCard with _buildGarminCard and _buildStravaCard methods
- Cards now read real connection status from healthConnectionsProvider
- Connected state shows: provider name, last sync time, attribution widget, Disconnect button with confirmation dialog
- Disconnected state shows: Connect button that launches OAuth flow (Garmin via Edge Function, Strava via direct URL)
- Added imports: url_launcher, health_connections_provider, garmin_attribution_widget, strava_attribution_widget
- Pull-to-refresh now also reloads provider connections
- Files changed: lib/features/health/presentation/health_connection_screen.dart
- **Learnings for future iterations:**
  - Attribution widgets use `visible` not `isVisible` parameter
  - healthConnectionsProvider exposes garminConnected/stravaConnected booleans + lastSync DateTimes
  - Garmin OAuth requires two-step flow: initiateGarminOAuth() for request token, then launchUrl
  - Strava OAuth uses direct URL from buildStravaAuthorizationUrl() helper
---

## 2026-03-28 - P11-002
- Verified Garmin OAuth flow end-to-end, found and fixed 4 issues:
  1. Edge Function had no 'initiate' action — added it to return OAuth 2.0 auth URL (client_id stays server-side)
  2. Repository connectGarmin expected `data['connection']` but EF returns `{status, garmin_user_id}` — fixed to re-fetch from DB
  3. Repository disconnect methods used POST but EF expects DELETE — fixed to use HttpMethod.delete
  4. Deep link handler extracted `oauth_verifier` (OAuth 1.0a) — fixed to also check `code` (OAuth 2.0)
- Also fixed connectStrava with same response parsing issue
- Provider initiateGarminOAuth now returns the full auth URL from Edge Function directly
- Files changed:
  - supabase/functions/oauth-garmin/index.ts (added initiate action)
  - lib/features/health/data/health_connections_repository.dart (response parsing + DELETE method)
  - lib/features/health/presentation/health_connections_provider.dart (doc update)
  - lib/app.dart (deep link handler: code + oauth_verifier)
- **Learnings for future iterations:**
  - Edge Functions route by HTTP method (POST/DELETE), not action field in body — except initiate which uses action field
  - Supabase functions.invoke defaults to POST; use `method: HttpMethod.delete` for DELETE
  - Edge Functions return minimal status responses, not full entities — re-fetch from DB after mutations
  - The actual Garmin integration uses OAuth 2.0 (not 1.0a despite comments)
---

## 2026-03-28 - P11-003
- Verified Strava OAuth 2.0 flow end-to-end — all components working correctly
- buildStravaAuthorizationUrl() correctly builds URL with client_id, redirect_uri, response_type=code, scope
- Deep link handler correctly catches welltrack://oauth/strava/callback and extracts code
- Edge Function exchanges code at https://www.strava.com/oauth/token for access_token + refresh_token
- Tokens stored encrypted in wt_health_connections with athlete metadata (firstname, lastname, athlete_id)
- UI updates to Connected state after successful OAuth via provider state
- Issues already fixed in P11-002: response parsing + HttpMethod.delete for disconnect
- No additional code changes needed
- **Learnings for future iterations:**
  - Strava webhook validation handles both GET (hub.challenge) and POST (event data)
  - Strava deauth endpoint: https://www.strava.com/oauth/deauthorize (best-effort)
  - Strava stores athlete_id as string in connection_metadata
---

## 2026-03-28 - P11-004
- Verified disconnect flow for both Garmin and Strava providers
- Garmin: Edge Function fetches token, calls Garmin user deregistration (best-effort), clears tokens, sets is_connected=false
- Strava: Edge Function fetches token, calls Strava deauthorize endpoint (best-effort), clears tokens, sets is_connected=false
- Repository uses HttpMethod.delete for both (fixed in P11-002)
- Provider updates UI state immediately (garminConnected/stravaConnected = false, lastSync = null)
- Health connection screen shows confirmation dialog before disconnect (added in P11-001)
- No additional code changes needed — all fixes made in P11-001 and P11-002
---

## 2026-03-28 - P11-005
- Verified token refresh for both Garmin and Strava
- refreshGarminToken() and refreshStravaToken() properly implemented in _shared/token-refresh.ts
- Both imported and called in backfill-health-data with 60-second expiry buffer
- Strava token refresh was already wired in webhook-processor.ts; added refreshGarminToken import
- Added markConnectionNeedsReauth() helper to both webhook-processor and backfill-health-data
- When token refresh fails, connection is now marked is_connected=false with needs_reauth metadata
- Files changed:
  - supabase/functions/_shared/webhook-processor.ts (import refreshGarminToken, add re-auth marking)
  - supabase/functions/backfill-health-data/index.ts (wrap refresh in try/catch, add re-auth marking)
- **Learnings for future iterations:**
  - Garmin webhook events push data in payload, no API call needed — only backfill needs tokens
  - Strava webhook events require fetching activity details from API — needs tokens
  - Strava ALWAYS rotates both tokens on refresh — must persist new refresh_token
  - 60-second buffer before expiry to account for request latency
---

## 2026-03-28 - P11-006
- Verified webhook ingestion pipeline for both Garmin and Strava
- Garmin webhook: validates HMAC-SHA1 signature (X-Garmin-Signature), queues to wt_webhook_events, responds 200 immediately
- Strava webhook: handles GET hub.challenge verification, queues POST events, responds 200 immediately
- process-webhooks: batch processes pending events, normalizes to wt_health_metrics with dedupe_hash
- Deregistration: Garmin handleGarminDeregistration() and Strava handleStravaDeauthorization() both mark is_connected=false
- No code changes needed — pipeline is correctly implemented
- **Learnings for future iterations:**
  - Garmin pushes data in webhook payload; Strava sends event notifications requiring API fetch
  - Strava doesn't sign individual webhook POSTs — security is via hub.challenge subscription verification
  - Garmin uses HMAC-SHA1 for request validation
  - All processing is async via wt_webhook_events queue, never inline in webhook handler
---

## 2026-03-28 - P11-007
- Verified AndroidManifest.xml has intent filters for welltrack://oauth/garmin/callback and welltrack://oauth/strava/callback
- Added triggerManualSync() to HealthConnectionsRepository — calls backfill-health-data Edge Function
- Added triggerManualSync() to HealthConnectionsNotifier with optimistic lastSync update
- Added Sync Now icon button to settings screen connected provider tiles
- Rate limit: Edge Function returns rate_limited status, shown as 'Already synced recently' SnackBar
- Loading spinner via isConnecting state, success/rate-limit SnackBar after
- Files changed:
  - lib/features/health/data/health_connections_repository.dart (triggerManualSync method)
  - lib/features/health/presentation/health_connections_provider.dart (triggerManualSync method)
  - lib/features/settings/presentation/settings_screen.dart (Sync Now button + _triggerSync)
- **Learnings for future iterations:**
  - backfill-health-data Edge Function has built-in 24h rate limit
  - Use IconButton with constraints: BoxConstraints() for compact icon buttons in ListTile trailing
---

## 2026-03-28 - P11-008
- Verified charts don't filter by source='health_connect' — all sources included
- Garmin stress data shown in stress chart, VO2 max from Garmin/Strava in VO2 chart
- Added body battery (Garmin-exclusive) to insights dashboard:
  - insights_provider.dart: fetches body_battery trend, includes in metricTrends map only when non-empty
  - insights_dashboard_screen.dart: body battery chart section shown conditionally when data exists
  - Uses teal color, 0-100 range (same as stress chart)
- Files changed:
  - lib/features/insights/presentation/insights_provider.dart (body_battery trend fetch)
  - lib/features/insights/presentation/insights_dashboard_screen.dart (body battery chart)
- **Learnings for future iterations:**
  - metricTrends is a Map<String, List<DataPoint>> — conditionally include keys for optional metrics
  - Body battery is Garmin-exclusive, only shows when Garmin data exists in wt_health_metrics
  - Source priority enforced in background sync: Garmin > Health Connect for stress/vo2max
---

## 2026-03-28 - P12-001
- Verified habit creation, toggle, and streak calculation — all criteria pass
- 4 preset habits: porn_free, kegels, sleep_target, steps_target — all display correctly
- Toggle calls logHabitDay() with optimistic UI update, reverts on failure
- Streak recalculation walks backwards from today counting consecutive completed days
- current_streak_days resets to 0 when a day is skipped (loop breaks at first gap)
- longest_streak_days only increases (uses max comparison)
- 30-day grid: green=completed, grey=incomplete, ring border=today
- No code changes needed — implementation is correct
---

## 2026-03-28 - P12-002
- Verified custom habit creation and soft delete — all criteria pass
- Custom habit creation: free-text TextField, auto-generated type key (custom_label_timestamp)
- Soft delete: sets is_active=false, preserves streak data and logs
- is_active filter in getActiveHabits() ensures deleted habits hidden on reload
- No code changes needed
---

## 2026-03-28 - P12-003
- Verified kegel timer with all 3 protocols and auto-logging — all criteria pass
- Quick Flicks: 3x10, 1s/1s, 10s rest ✓ | Long Holds: 3x5, 5s/5s, 15s rest ✓ | Reverse: 3x5, 5s/5s, 15s rest ✓
- Phase transitions correct: squeeze→relax→next rep/rest→next set/complete
- Timer.periodic(1s) drives countdown, phaseProgress property for ring
- Auto-logs kegels habit via habitRepo.logHabitDay() on session complete (best-effort)
- Pause cancels timer, resume restarts it
- No code changes needed
---

## 2026-03-28 - P12-004
- Verified bloodwork logging — all criteria pass
- 12 test types in 4 tabs (Hormones 3, Metabolic 2, Cardiovascular 5, Vitamins 2)
- Color indicators: green (in range), amber (borderline 10%), red (out of range)
- Edit via long-press, delete with confirmation dialog
- is_out_of_range is Postgres GENERATED column, correctly omitted from toJson()
- No code changes needed
---

## 2026-03-28 - P12-005
- Verified bloodwork detail trend chart and AI interpretation
- Fixed: reference range band shading was broken — BetweenBarsData referenced indices 1,2 but no invisible bound lines existed
- Added _buildRangeBoundLines() to create two invisible LineChartBarData entries for lower/upper bounds
- Chart handles single data point via _SingleValueDisplay fallback
- AI consent check via ai_consent_bloodwork profile field
- AI uses 'interpret_bloodwork' workflow via ai_orchestrator_service
- Deterministic fallback builds text from out-of-range/borderline results
- 3-second debounce via _lastAiCall timestamp
- Medical disclaimer always visible in amber box
- Files changed: lib/features/bloodwork/presentation/bloodwork_detail_screen.dart
- **Learnings for future iterations:**
  - FL Chart BetweenBarsData requires actual LineChartBarData entries at the referenced indices
  - Add invisible bound lines (barWidth: 0, color: transparent) to support between-bars shading
---

## 2026-03-28 - P12-006
- Connected habits and bloodwork to real data on main dashboard
- Replaced hardcoded "5-Day Streak!" with real habitProvider data (best active streak)
- Added bloodwork summary card showing flagged results count from bloodworkProvider
- Both cards navigate to /habits and /bloodwork on tap
- Both handle empty state (no habits → "Start tracking habits", no bloodwork → "Log your first blood test")
- Bloodwork card: red icon when flagged, green when all in range
- Files changed: lib/features/dashboard/presentation/dashboard_screen.dart
- **Learnings for future iterations:**
  - HabitEntity uses `habitLabel` not `displayName`
  - BloodworkState exposes `outOfRangeCount` and `results.length` for dashboard summaries
---

## 2026-03-28 - P12-007
- Verified RLS policies: all 4 tables use profile_id IN (SELECT id FROM wt_profiles WHERE user_id = auth.uid())
- Verified indexes: idx_habit_streaks_profile_active, idx_habit_logs_profile_type_date, idx_bloodwork_profile_test_date all present
- Fixed: context-builder.ts was not filtering by is_sensitive when fetching bloodwork for AI context — added .eq('is_sensitive', false)
- No print/debugPrint statements found in habits or bloodwork code
- is_sensitive field exists on wt_bloodwork_results with default true
- Files changed: supabase/functions/_shared/context-builder.ts
- **Learnings for future iterations:**
  - All bloodwork defaults to is_sensitive=true — must explicitly mark false to include in AI context
  - RLS pattern: profile_id IN (SELECT id FROM wt_profiles WHERE user_id = auth.uid())
---

## 2026-03-28 - P14-001
- Created lib/features/meals/presentation/today_nutrition_provider.dart:
  - todayMacroSummaryProvider(profileId) — consumed vs goal for protein, carbs, fat
  - todayCalorieSummaryProvider(profileId) — base/adjusted goals with recovery badge for PRO
  - todayMicronutrientSummaryProvider(profileId) — fat, sodium, cholesterol, carbs, sugar, fiber (null=untracked)
  - todayNutritionDashboardProvider(profileId) — combined single async state
  - todayStepsProvider(profileId) — from Health Connect/Garmin
- Created lib/features/insights/presentation/today_recovery_score_provider.dart:
  - todayRecoveryScoreProvider(profileId) — reads latestRecoveryScore from insightsProvider
- PlanType→dayType mapping: push→strength, normal→cardio, easy/rest→rest
- PRO users get MacroCalculator.applyRecoveryAdjustment with calorieModifier and calorieAdjustmentPercent
- **Learnings for future iterations:**
  - RecoveryScoreEntity uses `recoveryScore` not `score`
  - HealthRepository.getMetrics takes positional params (profileId, metricType)
  - MacroTargets is in macro_calculator.dart, not a separate file
  - currentPlanTierProvider is autoDispose — watch inside FutureProvider
---

## 2026-03-28 - P14-002
- Added smooth_page_indicator: ^1.1.0 to pubspec.yaml (resolved as 1.2.1)
- Created lib/shared/core/constants/fab_colors.dart with 4 const Color values:
  - kFabColorLogFood (green), kFabColorBarcode (blue), kFabColorVoice (purple), kFabColorMealScan (orange)
- flutter pub get succeeded, flutter analyze 0 issues
- Files changed: pubspec.yaml, pubspec.lock, lib/shared/core/constants/fab_colors.dart
- **Learnings for future iterations:**
  - smooth_page_indicator 1.2.1 resolves cleanly with current constraints
  - FAB color constants are top-level const, not in a class (matches Dart convention for simple constants)
---

## 2026-03-28 - P14-003
- Created NutritionSummaryCarousel with 4-page PageView + smooth_page_indicator dots
- Created 6 new widget files:
  - nutrient_progress_bar.dart — shared reusable bar for Heart Healthy / Low Carb pages
  - macro_rings_carousel_page.dart — Page 1: 3 CustomPainter rings (80x80) for protein/carbs/fat with "Xg left/over"
  - calories_budget_carousel_page.dart — Page 2: large ring (120x120) with Base Goal/Food/Exercise breakdown + recovery badge
  - heart_healthy_carousel_page.dart — Page 3: Fat/Sodium/Cholesterol progress bars, FreemiumGate wrapped
  - low_carb_carousel_page.dart — Page 4: Carbs/Sugar/Fiber progress bars, FreemiumGate wrapped
  - nutrition_summary_carousel.dart — container with PageView, dot indicator, shimmer loading, error/zero states
- Replaced hardcoded _buildNutritionCarousel and _buildMacroRing in dashboard_screen.dart
- Each page tappable → /nutrition?tab=macros|calories|heart|lowcarb
- Over-goal: ring color switches to amber when consumed > goal
- Zero-state: "No food logged today" with "Log your first meal" button → /meals/food-search
- Files changed:
  - lib/features/dashboard/presentation/widgets/nutrient_progress_bar.dart (new)
  - lib/features/dashboard/presentation/widgets/macro_rings_carousel_page.dart (new)
  - lib/features/dashboard/presentation/widgets/calories_budget_carousel_page.dart (new)
  - lib/features/dashboard/presentation/widgets/heart_healthy_carousel_page.dart (new)
  - lib/features/dashboard/presentation/widgets/low_carb_carousel_page.dart (new)
  - lib/features/dashboard/presentation/widgets/nutrition_summary_carousel.dart (new)
  - lib/features/dashboard/presentation/dashboard_screen.dart (modified)
- **Learnings for future iterations:**
  - SmoothPageIndicator WormEffect uses const constructor (dotColor/activeDotColor are const AppColors)
  - FreemiumGate wraps entire page content — acceptable for full-page gating inside carousel
  - CustomPainter rings: use drawArc with -pi/2 start angle for top-start
  - Dashboard NutritionSummaryCarousel is ConsumerStatefulWidget to own PageController lifecycle
  - todayNutritionDashboardProvider combines macros+calories+micro into single async state
---

## 2026-03-28 - P14-004
- Created StepsSummaryTile and ExerciseSummaryTile as ConsumerWidgets reading real provider data
- Added todayExerciseProvider to today_nutrition_provider.dart — combines active calories from wt_health_metrics + duration from completed workouts
- Steps tile: shows step count vs 10k target with progress ring, '-- steps' + Connect link when no health data
- Exercise tile: shows active calories + duration in minutes from completed workouts
- Replaced hardcoded _buildMetricsRow (static '12,450' / '850') with real data tiles
- Removed unused _buildGlassMetricCard method and dart:ui import from dashboard_screen.dart
- Both tiles side-by-side in Row with Expanded children, glassmorphism style matching existing design
- Files changed:
  - lib/features/meals/presentation/today_nutrition_provider.dart (ExerciseSummary class + todayExerciseProvider)
  - lib/features/dashboard/presentation/widgets/steps_summary_tile.dart (new)
  - lib/features/dashboard/presentation/widgets/exercise_summary_tile.dart (new)
  - lib/features/dashboard/presentation/dashboard_screen.dart (wired tiles, removed old methods)
- **Learnings for future iterations:**
  - WorkoutEntity has durationMinutes but no caloriesBurned — exercise calories come from MetricType.calories in wt_health_metrics
  - workoutRepositoryProvider exposes getTodayWorkouts(profileId) for same-day workout queries
  - todayStepsProvider already existed in today_nutrition_provider.dart from P14-001
---
