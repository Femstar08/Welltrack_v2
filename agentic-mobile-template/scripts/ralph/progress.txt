# Ralph Progress Log — Phase 13: UX Refinement & Critical Fixes
Started: 2026-03-12
Branch: ralph/phase-13-ux-refinement
---

## Codebase Patterns
- All navigation uses GoRouter — NEVER use Navigator.pushNamed()
- Dashboard route is `/` not `/dashboard`
- All Supabase calls must go through repository classes
- Riverpod providers: use .family keyed by profileId for user-scoped state
- After mutations, invalidate downstream providers with ref.invalidate()
- AI calls go through ai_orchestrator_service.dart — never direct from screens
- Error handling: wrap async in try/catch with AppLogger
- All entities use Freezed for immutability
- Use context.push() for stack navigation, context.go() for replacement
- All screens pushed via context.push() MUST have AppBar with back button
---

## Exploration Notes
- 67 routes defined in app_router.dart
- 40 Riverpod providers across the codebase
- 108 files changed in latest commit
- NotificationService exists but initialize() never called
- dashboard_provider._loadRecoveryScore is a Future.delayed(500ms) stub
- PrescriptionEngine bedtime formula has documented off-by-one bug
- 6 pushed screens have no back button
- AI calls in bloodwork/insights/checkin have no debounce
- MorningCheckInNotifier.submit() makes 5 sequential independent queries
- OverloadDetectionService has N+1 query pattern
---
