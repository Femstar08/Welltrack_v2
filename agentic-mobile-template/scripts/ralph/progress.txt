# Ralph Progress Log — Architectural Debt
Started: 2026-03-29
Branch: ralph/review-fixes

## Codebase Patterns
- Use cmd.exe /c to run Flutter commands on WSL2
- All navigation uses GoRouter — routes in app_router.dart
- All Supabase calls through repository classes — screens use providers only
- activeProfileIdProvider in app_router.dart
- profileRepositoryProvider for profile access
- SyncEngine + OfflineQueue exist in shared/core/sync/
- ConnectivityService provides online/offline stream
- Hive for offline queue storage
---

## 2026-03-30 - ARCH-005
- Split app_router.dart from 900+ lines to ~260 lines
- Created routes/dashboard_routes.dart with dashboardChildRoutes(ref) — ~50 routes
- Created routes/workout_routes.dart with workoutChildRoutes(ref) — 6 routes
- app_router.dart now contains: providers, auth refresh, redirect logic, branch structure
- Route definitions fully extracted to route files
- Re-added goRouterProvider that was lost during rewrite
- All 530 tests pass, flutter analyze clean
- Files changed:
  - lib/shared/core/router/app_router.dart (rewritten)
  - lib/shared/core/router/routes/dashboard_routes.dart (new)
  - lib/shared/core/router/routes/workout_routes.dart (new)
---

## 2026-03-30 - ARCH-006
- Audited meal_plan_screen.dart — 6 Supabase.instance.client.auth.currentUser?.id usages are read-only auth accessors (acceptable)
- All repository access already goes through providers: ref.read(profileRepositoryProvider), ref.read(mealPlanProvider)
- MacroCalculator and CustomMacroTargetRepository are used further in the file (lines 471, 484) — imports needed
- No direct repository instantiations found — screen follows the pattern correctly
- No code changes needed beyond verification
---
