# Ralph Progress Log — Review Fixes
Started: 2026-03-29
Branch: ralph/review-fixes
Source: Comprehensive 5-agent review (Design, Security, BA, PM, Architecture)

## Codebase Patterns
- Use `cmd.exe /c` to run Flutter commands on WSL2
- Must pass `--dart-define-from-file=.env` for Supabase credentials
- All navigation uses GoRouter — NEVER use Navigator.pushNamed()
- Dashboard route is `/` not `/dashboard` — use `context.go('/')`
- `wt_users` has NO email column — get email from `auth.currentUser`
- All Supabase calls must go through repository classes
- Riverpod providers: use .family keyed by profileId for user-scoped state
- After mutations, invalidate downstream providers with ref.invalidate()
- AI calls go through ai_orchestrator_service.dart — never direct from screens
- Use context.push() for stack navigation, context.go() for replacement
- All screens pushed via context.push() MUST have AppBar with back button
- PrescriptionEngine is static class, not a provider
- Recovery score accessed via insightsProvider, not standalone provider
- FreemiumGate uses context.push('/paywall') for navigation (fixed in P15-002)
- Freemium gates use `currentPlanTierProvider` (FutureProvider)
- `onboarding_completed` is on `wt_users`, NOT `wt_profiles`
- minSdk = 28, AGP 8.11.1, Kotlin 2.2.20
- AppColors has recovery color tokens: recoveryExcellent/Good/Moderate/Low/Critical
- ref.watch must NOT be used after await in async providers — use ref.read
- GoRouter needs refreshListenable for reactive auth guard re-evaluation
---

## 2026-03-29 - SEC-001
- Removed hardcoded defaultValue for supabaseUrl and supabaseAnonKey from api_constants.dart
- Build now fails at runtime if --dart-define-from-file=.env is not passed (intentional)
- Verified no other hardcoded Supabase URLs in lib/ directory
- Files changed: lib/shared/core/constants/api_constants.dart
---

## 2026-03-29 - SEC-002
- Created migration 20260329000001_revoke_sensitive_columns.sql
- REVOKE UPDATE on plan_tier and onboarding_completed for authenticated role
- Created mark_onboarding_complete() RPC with SECURITY DEFINER — validates profile exists before setting flag
- Updated profile_repository.dart to use _client.rpc('mark_onboarding_complete') instead of direct UPDATE
- Removed updatePlanTier() from freemium_repository.dart — tier changes must be server-side only
- Files changed:
  - supabase/migrations/20260329000001_revoke_sensitive_columns.sql (new)
  - lib/features/freemium/data/freemium_repository.dart
  - lib/features/profile/data/profile_repository.dart
- **Learnings:** Supabase column-level REVOKE works with PostgREST. Use SECURITY DEFINER RPC functions to allow specific operations that bypass the REVOKE.
---

## 2026-03-29 - SEC-003
- Added generateOAuthState() using Random.secure() + base64Url (256-bit nonce)
- Added pendingOAuthStateProvider to store state before launching OAuth
- Updated buildStravaAuthorizationUrl() and buildGarminAuthorizationUrl() to accept state parameter
- Updated deep link handler in app.dart to verify returned state matches expected state
- State mismatch logs warning and rejects the callback (CSRF protection)
- Updated settings_screen.dart and health_connection_screen.dart to generate + store state before OAuth
- Added Ref parameter to HealthConnectionsNotifier for state provider access
- Files changed:
  - lib/features/health/presentation/health_connections_provider.dart
  - lib/app.dart
  - lib/features/settings/presentation/settings_screen.dart
  - lib/features/health/presentation/health_connection_screen.dart
---
